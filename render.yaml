services:
  - type: web
    name: dnd-nexus
    env: python
    plan: free
    buildCommand: "./build.sh"
    startCommand: "cd api && uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DATABASE_URL
        sync: false
